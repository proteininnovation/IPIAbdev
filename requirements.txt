# ══════════════════════════════════════════════════════════════════════════════
# DELPHI — Requirements
# Institute for Protein Innovation (IPI)
#
# Install all dependencies:
#   pip install -r requirements.txt
#
# Note: ANARCI and HMMER must be installed via conda (hmmer is installed explicitly
#       to guarantee it is present, as conda dependency resolution is not always reliable):
#   conda install -c bioconda hmmer anarci
#
# Note: IgBERT weights are downloaded automatically from HuggingFace Hub
#   (Exscientia/IgBert) on first use — no separate install needed.
# ══════════════════════════════════════════════════════════════════════════════

# ── Data handling ─────────────────────────────────────────────────────────────
numpy
pandas
scipy
openpyxl
pyyaml

# ── ML frameworks ─────────────────────────────────────────────────────────────
# torch is installed separately in install.sh via pip wheel from pytorch.org
# to avoid Intel MKL conflicts (iJIT_NotifyEvent error on some Linux systems).
# Do not add torch here — it will conflict with the GPU-specific wheel.
scikit-learn
xgboost==2.1.1
joblib

# ── Interpretability ──────────────────────────────────────────────────────────
captum
shap

# ── Visualisation ─────────────────────────────────────────────────────────────
matplotlib
seaborn
python-pptx

# ── Sequence utilities ────────────────────────────────────────────────────────
Levenshtein
tqdm

# ── Protein language models ───────────────────────────────────────────────────
ablang2
antiberty
transformers
rjieba                      # required by some HuggingFace tokenizers (IgBERT, AntiBERTa2)
peft
tf-keras

# ── Experiment tracking ───────────────────────────────────────────────────────
mlflow
